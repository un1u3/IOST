#include <stdio.h>

int main()
{
    float P = 400, T = 2, R = 5.5;
    float SI = (P * T * R) / 100;
    printf("\n The Simple Interest is %.4f.", SI);
    printf("\n --[Your name]--");

    return 0;
}
